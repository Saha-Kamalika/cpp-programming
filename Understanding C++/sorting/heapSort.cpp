/* convert the array to a complete binary tree
perform heapify to convert it into either a min heap or max heap
perform heap sort--
    remove the root element and replace it with the last node of the heap and then heapify again
*/